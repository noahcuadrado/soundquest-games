# Client-side LLM Configuration
# Note: These will be exposed in the browser bundle
# For production, consider using a backend proxy to hide API keys

# OpenAI API Key (primary provider for lesson generation)
# The app now uses OpenAI directly with the gpt-4o-mini model
VITE_OPENAI_API_KEY=your-openai-api-key-here

# OpenRouter API Key (fallback option)
# Can be used as an alternative provider if needed
VITE_OPENROUTER_API_KEY=your-openrouter-key-here

# Server-side LLM Configuration (for Netlify Functions)
# OpenAI API Key for server-side generation
OPENAI_API_KEY=your-openai-api-key-here

# OpenRouter API Key for server-side generation (fallback)
OPENROUTER_API_KEY=your-openrouter-key-here

# LLM Provider Selection (openai or openrouter)
# Default is now 'openai' for better performance and reliability
LLM_PROVIDER=openai

# Stripe Payment Link (safe to expose)
VITE_STRIPE_PAYMENT_LINK=https://buy.stripe.com/your-link-here

# Neon Database Connection (for Netlify Function)
# Get this from your Neon.tech dashboard (use the pooled connection string)
NEON_DATABASE_URL=postgresql://username:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require